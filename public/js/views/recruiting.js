import { placeholder } from '../components/ui.js';
export async function render(container) {
  placeholder(container, 'Recruiting', 'Scout and recruit incoming freshman classes starting in Phase 6.');
}
