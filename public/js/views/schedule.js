import { placeholder } from '../components/ui.js';
export async function render(container) {
  placeholder(container, 'Schedule', 'Your 22-game season and playoff bracket appear once a career is created.');
}
