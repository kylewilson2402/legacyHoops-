import { placeholder } from '../components/ui.js';
export async function render(container) {
  placeholder(container, 'Dashboard', 'Your program at a glance — record, next game, leaders, and news land in later phases.');
}
