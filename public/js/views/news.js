import { placeholder } from '../components/ui.js';
export async function render(container) {
  placeholder(container, 'News', 'Headlines react to your results and recruiting starting in Phase 7.');
}
