import { placeholder } from '../components/ui.js';
export async function render(container) {
  placeholder(container, 'Standings', 'League standings compute from played games starting in Phase 5.');
}
