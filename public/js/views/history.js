import { placeholder } from '../components/ui.js';
export async function render(container) {
  placeholder(container, 'League History', 'Champions, dynasty timelines, and records accumulate from Phase 8.');
}
